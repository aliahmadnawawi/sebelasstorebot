environment:
  DATABASE_URL: ${DATABASE_URL}
  REDIS_URL: ${REDIS_URL}
  PAKASIR_PROJECT: ${PAKASIR_PROJECT}
  PAKASIR_API_KEY: ${PAKASIR_API_KEY}
  BOT_TOKEN: ${BOT_TOKEN}
  ADMIN_TELEGRAM_ID: ${ADMIN_TELEGRAM_ID}
  QRIS_EXPIRE_MINUTES: ${QRIS_EXPIRE_MINUTES}

